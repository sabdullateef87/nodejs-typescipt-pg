describe("users", () => {
    it("should test our stuffs", () => {
        expect(1).toBe(1);
    });
    it("should be able to figure out our runner", () => {
        expect("john").toBe("john");
    });
});
//# sourceMappingURL=first.test.js.map